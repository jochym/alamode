version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: ls -l
      - run:
          name: Print the Current Time
          command: g++ -V
